# SQLite database (local file)
DATABASE_URL="file:./dev.db"

# --- WhatsApp via Twilio (optional — console fallback used when empty) ---
# Get sandbox credentials at https://console.twilio.com (Messaging → Try WhatsApp)
TWILIO_ACCOUNT_SID=""
TWILIO_AUTH_TOKEN=""
TWILIO_WHATSAPP_FROM="whatsapp:+14155238886"
ALERT_WHATSAPP_TO="whatsapp:+972500000000"

# Scheduler interval (minutes)
SCAN_INTERVAL_MIN="5"
